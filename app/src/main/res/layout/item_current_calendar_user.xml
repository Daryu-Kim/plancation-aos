<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:id="@+id/side_item_current_calendar_user_layout"
        android:background="@android:color/transparent"
        android:layout_marginBottom="8dp"
        android:paddingVertical="2dp"
        android:paddingStart="12dp"
        android:layout_height="wrap_content">
    <RelativeLayout
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:id="@+id/side_item_current_calendar_user_img"
            android:layout_centerVertical="true"
            android:layout_alignParentStart="true"
            android:background="@drawable/ic_user_profile">
        <TextView android:layout_width="match_parent" android:layout_height="wrap_content"
                  android:text="Administrator"
                  android:gravity="center"
                  android:layout_centerInParent="true"
                  android:id="@+id/side_item_current_calendar_user_img_name"
                  android:textColor="@color/inverse_text"
                  android:maxLines="1"
                  android:maxLength="3"
                  android:textSize="12sp"
                  android:fontFamily="@font/bold"/>
    </RelativeLayout>
    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
              android:text="Administrator"
              android:gravity="center"
              android:id="@+id/side_item_current_calendar_user_name"
              android:textColor="@color/text"
              android:maxLines="1"
              android:layout_centerVertical="true"
              android:layout_marginStart="12dp"
              android:layout_toEndOf="@id/side_item_current_calendar_user_img"
              android:ellipsize="end"
              android:textSize="14sp"
              android:fontFamily="@font/medium"/>

</RelativeLayout>